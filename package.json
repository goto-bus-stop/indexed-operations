{
  "name": "indexed-operations-workspace",
  "description": "",
  "author": "Renée Kooi <renee@kooi.me>",
  "keywords": [],
  "license": "MIT OR Apache-2.0",
  "private": true,
  "scripts": {
    "build": "(cd indexed-operations && tsdown --dts) && (cd indexed-turf && tsdown --dts)",
    "test": "vitest run && tsc -p ."
  },
  "type": "module",
  "devDependencies": {
    "tsdown": "^0.18.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16"
  }
}
